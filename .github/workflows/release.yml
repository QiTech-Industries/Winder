name: Create Release

on:
  workflow_dispatch:

jobs:
  call-workflow-1:
    uses: QiTech-Industries/Winder/.github/workflows/pio.yml@master
  call-workflow-2:
    uses: QiTech-Industries/Winder/.github/workflows/wspiffs.yml@master